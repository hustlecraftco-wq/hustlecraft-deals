// app/page.tsx
import { Suspense } from 'react';
import DealsGrid from '@/components/DealsGrid';
import SearchBar from '@/components/SearchBar';
import StatsBar from '@/components/StatsBar';

export default function HomePage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950">
      {/* Hero Section */}
      <section className="relative overflow-hidden border-b border-slate-800">
        <div className="absolute inset-0 bg-gradient-to-br from-blue-600/10 via-transparent to-purple-600/10" />
        <div className="relative max-w-7xl mx-auto px-4 py-20 sm:px-6 lg:px-8">
          <div className="text-center space-y-6">
            <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-blue-500/10 border border-blue-500/20 text-blue-400 text-sm font-medium mb-4">
              <span className="relative flex h-2 w-2">
                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
                <span className="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
              </span>
              Live deals updating every 30 minutes
            </div>
            
            <h1 className="text-5xl sm:text-7xl font-bold text-white tracking-tight">
              HustleCraft Deals
            </h1>
            
            <p className="text-xl sm:text-2xl text-slate-400 max-w-2xl mx-auto leading-relaxed">
              Catch it before it's gone. A live feed of real savings—scored for signal, 
              not hype—so you buy smart and fast.
            </p>

            <div className="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4">
              <button className="px-8 py-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-all hover:scale-105 shadow-lg shadow-blue-500/25">
                Browse Today's Deals
              </button>
              <button className="px-8 py-4 bg-slate-800 hover:bg-slate-700 text-white font-semibold rounded-lg transition-all border border-slate-700">
                Get Free Playbook
              </button>
            </div>
          </div>
        </div>
      </section>

      <StatsBar />
      
      <section className="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
        <SearchBar />
      </section>

      <section className="max-w-7xl mx-auto px-4 pb-20 sm:px-6 lg:px-8">
        <Suspense fallback={<DealsGridSkeleton />}>
          <DealsGrid />
        </Suspense>
      </section>

      <section className="max-w-7xl mx-auto px-4 pb-20 sm:px-6 lg:px-8">
        <div className="relative overflow-hidden rounded-2xl bg-gradient-to-r from-blue-600 to-purple-600 p-12 text-center">
          <div className="relative z-10">
            <h2 className="text-3xl sm:text-4xl font-bold text-white mb-4">
              Want the method behind these picks?
            </h2>
            <p className="text-lg text-blue-100 mb-8 max-w-2xl mx-auto">
              Get a free chapter + checklist from the HustleCraft Playbook.
            </p>
            <button className="px-8 py-4 bg-white text-blue-600 font-semibold rounded-lg hover:bg-blue-50 transition-all hover:scale-105 shadow-lg">
              Read the Free Chapter →
            </button>
          </div>
        </div>
      </section>
    </div>
  );
}

function DealsGridSkeleton() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {[...Array(6)].map((_, i) => (
        <div key={i} className="bg-slate-800 rounded-xl h-96 animate-pulse" />
      ))}
    </div>
  );
}
